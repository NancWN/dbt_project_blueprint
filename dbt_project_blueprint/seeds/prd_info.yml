version: 2

seeds:
  - name: prd_info
    description: product info 
    config:
      column_types:
          prd_id: int
          prd_key: varchar(50)
          prd_nm: varchar(50)
          prd_cost: int
          prd_line: varchar(50)
          prd_start_dt: date
          prd_end_dt: date
